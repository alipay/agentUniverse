info:
  name: 'map_agent'
  description: 'map agent'
profile:
  prompt_version:
  llm_model:
    name: 'default_openai_llm'
    model_name: 'gpt-4'

plan:
  planner:
    name: 'map_reduce_planner'
    map_agent: 'map_agent'
    reduce_agent: 'rag_agent'
metadata:
  type: 'AGENT'
  module: 'agentuniverse.agent.default.map_reduce_agent.map_reduce_agent'
  class: 'MapReduceAgent'