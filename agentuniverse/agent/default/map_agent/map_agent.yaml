info:
  name: 'map_agent'
  description: 'map agent'
profile:
  prompt_version:
  llm_model:
    name: 'default_openai_llm'
    model_name: 'gpt-4'

plan:
  planner:
    name: 'map_planner'
metadata:
  type: 'AGENT'
  module: 'agentuniverse.agent.default.map_agent.map_agent'
  class: 'MapAgent'